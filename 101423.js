noise(time, 1, 1)
	.modulateScrollX(osc(100, 1, ))
	.scale(() => Math.PI / 1000, 1)
	.add(noise()
		.rotate(time, 1)
		.modulate(noise(1, 1, 1))
		.repeat(time)
		.scale(Math.PI, 1)
		.mask(noise(1, 1, 1)
			.pixelate()
			.scale(time))
		.color())
	.mask(shape(8, 0.1)
		.scale(() => time))
	.out(o0)
render(o0)
